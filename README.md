# Small examples for using Rack-Test

## Background
There might be cases where no full-fledged Rails application is needed (e.g. in protocol testing). This project examines a bit the options of testing rack apps

